<h1>Listing wods</h1>

<table>
  <tr>
    <th> name</th>
    <th>Repetitions</th>
    <th>Weightamount</th>
    <th>Timealotted</th>
    <th>Numberofdaysperweek</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @wods.each do |wod| %>
  <tr>
    <td><%= wod. Name %></td>
    <td><%= wod.Repetitions %></td>
    <td><%= wod.WeightAmount %></td>
    <td><%= wod.TimeAlotted %></td>
    <td><%= wod.NumberOfDaysPerWeek %></td>
    <td><%= link_to 'Show', wod %></td>
    <td><%= link_to 'Edit', edit_wod_path(wod) %></td>
    <td><%= link_to 'Destroy', wod, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Wod', new_wod_path %>

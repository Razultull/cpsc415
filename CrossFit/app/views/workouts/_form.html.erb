<%= form_for(@workout) do |f| %>
  <% if @workout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workout.errors.count, "error") %> prohibited this workout from being saved:</h2>

      <ul>
      <% @workout.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :workout_length %><br />
    <%= f.text_field :workout_length %>
  </div>
  <div class="field">
    <%= f.label :number_of_days_per_week %><br />
    <%= f.number_field :number_of_days_per_week %>
  </div>
  <div class="field">
    <%= f.label :number_of_exercises %><br />
    <%= f.number_field :number_of_exercises %>
  </div>
  <div class="field">
    <%= f.label :creator_id %><br />
    <%= f.text_field :creator_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
